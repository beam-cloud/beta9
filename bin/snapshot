#!/usr/bin/python3
# -*- coding: utf-8 -*-
import os

from beta9 import Sandbox, SandboxInstance

sandbox: SandboxInstance = Sandbox().connect(os.getenv("CONTAINER_ID"))
image_id = sandbox.create_image_from_filesystem()
print("Created image from filesystem: ", image_id)
